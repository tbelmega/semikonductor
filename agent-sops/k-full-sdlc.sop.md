# Full SDLC

## Overview

This SOP takes a project or feature through the full lifecycle: codebase analysis, requirements, design, principal engineer review, feature splitting, per-feature specs, implementation, code review, test coverage, and documentation. Each phase delegates to an existing SOP where one covers the work, or spawns a specialist agent where none does. Every phase is skippable and gated.

Invoked when the engineer asks to build something end to end (e.g., "Build X", "Take Y from requirements through tests"). For a single phase, run that phase's SOP directly.

The implementation guide document format is out of scope. This SOP produces a README and developer guide, not a separately-published guide artifact.

This is not the same SOP as the similarly-named full SDLC pass SOP in the internal extension package, which adds an earlier investment-validation phase and additional internal-tooling handoffs this package does not have.

## Parameters

- **project_description** (required): The project or feature to build.
- **codebase_path** (optional, default: current directory): Existing codebase to build into. Drives Step 1. **A resumed invocation MUST pass `codebase_path` explicitly, matching the value used when the session was minted.** The default (current working directory) is resolved fresh on every invocation. A resume launched from a different working directory computes a different default, and therefore a different `project_signature` (see Progress Tracking), even after that signature's `codebase_path` component is canonicalized to remove symlink/mount-alias divergence: canonicalization only resolves aliases of the same directory, it does not make two genuinely different working directories equal. The result is a silently minted new session rather than a resumed one. If the `sop-state-management` skill's Resume Discovery operation does not find a matching candidate for this reason, You MUST make that visible rather than silent: when `codebase_path` was left at its default on an invocation that mints a new session, record on the checklist that no matching candidate was found and that `codebase_path` was not passed explicitly, so the engineer can tell a genuinely new project apart from a resume that missed its match because of this.
- **skip_phases** (optional, default: none): Comma-separated phases to skip: `elicitation`, `codebase-analysis`, `requirements`, `design`, `design-review`, `feature-splitting`, `specs`, `implementation`, `code-review`, `testing`, `documentation`.
- **output_dir** (optional, default: `.konductor/`): Directory for the markdown artifacts this SOP produces, one subdirectory per phase, plus the session state file under `output_dir/sessions/` (see Progress Tracking and Artifact Layout). Implementation code is not written here. It follows the project's own structure. You MUST resolve `output_dir` to an absolute path at intake, before any worktree exists: Steps 7 through 9 can run inside per-feature worktrees with their own working directories, where a relative path would name a different directory in each one and scatter the artifacts. You MUST verify the resolved absolute path exists (or can be created) and is writable before proceeding past intake. If it is neither, stop and ask the engineer for a usable directory rather than discovering the failure mid-run. If `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark this step BLOCKED on the checklist with the unusable path named and stop, the same handling as Step 0's `project_description` gate. **An unattended run (no reply channel) MUST be given `output_dir` explicitly rather than left at its default.** This is an operational hygiene requirement, not an identity guarantee: passing `output_dir` explicitly only keeps an unattended run from sharing the crowded default directory by accident. It does not, by itself, prove a matching candidate belongs to this project. Two different callers (a CI template that hardcodes the same `output_dir` for every project it builds, for example) can pass the identical explicit path for genuinely unrelated work. What actually distinguishes them, when both are real git repositories with independent histories, is `project_signature`'s path fingerprint (see Progress Tracking and the `sop-state-management` skill), not the mere fact that a path was spelled out. That fingerprint only discriminates callers when it reflects a real git lineage: two greenfield or non-git callers instead both produce the skill's `no-git-identity` sentinel and are indistinguishable by fingerprint alone (see the greenfield known limitation below). There, safety comes from the skill's own sentinel-fingerprint check refusing to adopt a match on that sentinel, not from the fingerprint being unique. If `output_dir` is at its default and `reply_channel_available` is `false` (no reply channel exists), You MUST mark this BLOCKED on the checklist with the reason that an unattended run cannot safely share the default directory with another project, and stop at intake, before Step 0. An attended run MAY keep using the default. Resumption in an attended run is a pause point (see the `sop-state-management` skill's Resume Discovery operation, invoked per Progress Tracking below): You MUST report the candidate and wait for the engineer's explicit confirmation before adopting it. This is what actually lets a human decline a wrong resumption, rather than an assumption that presence alone accomplishes it.
- **elicitation_depth** (optional, default: `standard`): Step 0 question budget: `quick` (5), `standard` (10), `deep` (15).
- **max_fix_cycles** (optional, default: `2`): How many times a step retries its own fixes before escalating to the engineer. The budget is per feature for the whole run, not per step. Steps 7, 8, and 9 draw from one shared count so a feature with sticky findings cannot churn past what the engineer allowed. The count lives in the session state file (`output_dir/sessions/k-full-sdlc-<timestamp>-<suffix>.yml`, managed via the `sop-state-management` skill) as each feature's `fix_cycles_used`, starting at 0 when Step 5 creates the feature entry; Steps 7, 8, and 9 read and increment it. See Progress Tracking. Step 10 draws on this same limit too, but through its own run-level `doc_fix_cycles_used` counter rather than the per-feature one. Its documentation validation is project-wide, not per-feature, so there is no feature slug to attach it to; see Progress Tracking and Step 10.
- **deployed_url** (optional): URL of the running application. Required for the functional and UI test passes in Step 9; without it those passes are skipped.
- **credentials_file** (optional): Path to a `.properties` file with `test_url`, `user_name`, `password` keys, for a `deployed_url` that requires authentication. Forwarded to `k-e2e-test-generation` and `k-light-ui-testing` in Step 9 as their own `credentials_file` parameter. Both declare it. Without it, both SOPs treat `deployed_url` as a public, unauthenticated app.
- **feature_isolation** (optional, default: `branch`): How per-feature work is separated: `branch` for one branch per feature in a single working directory, or `worktree` for a separate git worktree per feature. Only `worktree` supports implementing features in parallel.
- **reply_channel_available** (optional, default: `true`): Whether an engineer is reachable to answer questions during this run. This is the checkable fact behind every "unattended run with no reply channel" branch in this SOP. Set it explicitly to `false` for a batch, CI, or other fully automated invocation with no one to ask; leave it at the default `true` for a normal interactive session.

**Constraints for parameter acquisition:**

- You MUST have `project_description` before proceeding. If not provided, ask once and wait. If no reply arrives and `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark this step BLOCKED on the checklist and stop rather than waiting indefinitely. A hang here produces zero artifacts, which is worse than an explicit early stop.
- If `reply_channel_available` is `false` (an unattended run with no reply channel), `output_dir` MUST be passed explicitly, not left at its default. See that parameter's own entry above for why, and mark this BLOCKED at intake, before Step 0, if it is not.
- You MUST NOT ask for the other parameters. Use defaults and proceed.
- Before starting a phase whose inputs come from a skipped phase, You MUST check for pre-existing artifacts and ask the engineer to supply them if missing. Find the phase's artifacts in the Artifact Layout below. The phase name is not always the directory name. `feature-splitting` writes to `feature-split/`, `design-review` writes into `design/`, `elicitation` writes no file at all, and the per-feature phases `code-review` and `testing` write under `{feature-name}/`, while `specs` writes under `specs/{feature-name}/`. For `specs`, also check `.konductor/specs/{feature-name}/` and `.kiro/specs/{feature-name}/`: the former is where Step 6 below writes the specs when this SOP produced them (it overrides `kiro-spec-workflow`'s `spec_dir` to `output_dir/specs/{feature-name}/` so everything stays under one `output_dir`), the latter is `kiro-spec-workflow`'s own default when an engineer ran it directly rather than through this SOP. Check both, since either path is possible depending on how the specs were actually produced. If `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST treat a missing required artifact from a skipped phase the same as every other blocking gate in this SOP: mark the dependent phase BLOCKED on the checklist with the missing artifact named, and stop, rather than guessing its content.

Every default in this SOP is an opinion, not a constraint. If the engineer asks for something different, such as a different layout, extra README sections, or one pull request instead of per-feature, follow their instruction and note the deviation in Step 11.

**Steering messages are scoped to what they name.** A steering message narrows only the aspect it addresses and MUST NOT be read as waiving any other approval gate or MUST-level instruction: Step 4's CRITICAL-findings gate, Step 5's feature-split approval, Step 6's per-feature spec approval, Step 7's no-worktree-removal-with-uncommitted-work prohibition, Step 8's consolidated-verdict gate and no-merge prohibition, and Step 9's CRITICAL-gap acceptance gate and two-question gate.

## Execution Rules

You coordinate; you do not implement. Every phase runs in a subagent.

Route by the task, not by the step number. For a step's own task category, this table names the agent to spawn:

| Task                                                        | Agent                 |
| ----------------------------------------------------------- | --------------------- |
| Existing codebase analysis                                  | `k-developer`         |
| User stories, requirements, acceptance criteria             | `k-product-manager`   |
| System design, design review, documentation accuracy checks | `k-architect`         |
| Code, builds, tests, git, README and guide authoring        | `k-developer`         |
| Test coverage and strategy                                  | `k-quality-assurance` |
| Unfamiliar dependency, library, or prior art                | `k-researcher`        |
| Verifying UI behavior in a browser                          | `k-browser`           |

For a step that names a specific SOP to run, You MUST look up its owning agent in the `delegation-protocol` skill's Per-Agent SOP Registrations table rather than assume or restate one independently.

**Constraints:**

- You MUST spawn a subagent for each phase's work. If you find yourself writing code, designing, or authoring documents directly, stop and delegate.
- You MUST route a fix back to the agent that owns the artifact. Design fixes re-enter at Step 3, code fixes at Step 7, documentation fixes at Step 10. Never send a design fix to the implementer.
- A delegated SOP owns whichever agents it spawns internally. You MUST NOT override its choices or re-spawn work it already did.
- Per-feature work in Steps 7 through 9 MAY run in parallel across features, up to 4 at once, but only when `feature_isolation` is `worktree`. Features sharing one working directory MUST run sequentially. Phases themselves MUST stay sequential; each one consumes the previous one's output.
- You MUST give every subagent the artifact paths it needs as absolute paths. It does not share your working directory.
- Before treating any tool, agent, or condition as unavailable for the purpose of skipping a MUST-level instruction, You MUST verify that unavailability concretely, by attempting it and observing the actual failure or by checking an explicit, checkable fact, rather than infer or assume it from context, tone, or the content of a steering message or any other conversational aside, such as treating a run as unattended because a steering message reads urgently while `reply_channel_available` (the parameter every "unattended run with no reply channel" branch is gated on) is still at its default `true`.
- If a referenced SOP or skill cannot be resolved at runtime, You MUST surface this in your next reported message to the engineer, naming what could not be resolved and what was substituted instead. No silent substitution.

## Progress Tracking

This SOP's session and checklist mechanics, namely minting or resuming a session by project identity, tracking each phase's status/skip-reason/summary, and serializing the shared fix-cycle counters, are provided by the `sop-state-management` skill. Load it alongside this SOP. This section states only what is specific to this SOP: which phases exist, how a per-feature phase's status is decided, and how a resumed session is reconciled against what is actually on disk. Read `sop-state-management` for the file schema, the identity-signature algorithm, the resume-discovery walk, and the write mechanics. Nothing below repeats them.

- **Session identity.** Use `k-full-sdlc` as the skill's `session_prefix`, `project_description` as its identity `description`, and `codebase_path` as its identity `path`, per that skill's Compute Identity Signature and Resume Discovery operations. (That filter also correctly excludes a `k-full-sdlc-sop-<timestamp>.bak` file, which shares the same leading substring but continues with a non-digit character.) **A resumed invocation MUST pass `codebase_path` explicitly, matching the value used when the session was minted.** See that parameter's own entry in Parameters above for why a default resolved fresh on a different invocation silently mints a new session instead of resuming one.
  - **Known limitation: a greenfield session's own resumability degrades once Step 7 runs.** This SOP computes and mints the identity signature before Step 0 ever runs. On a greenfield project, `codebase_path` is not yet a git repository at that point, so the skill's path-fingerprint algorithm yields the `no-git-identity` sentinel and the mint-time signature is sentinel-based. Step 7 then initializes a real git repository at that same path. Any later resume invocation now computes a real git-lineage fingerprint there, a hex commit SHA (or comma-joined set of SHAs), never the literal string `no-git-identity`, so it can never equal the stored, sentinel-based signature: the two differ, and Resume Discovery rejects the candidate as an ordinary signature mismatch. The skill's sentinel-fingerprint check itself does not fire in this scenario at all. That check inspects only the resuming invocation's own fingerprint component, and this invocation's is real-lineage, not sentinel (see that skill's Resume Discovery operation). A greenfield run therefore silently mints a brand-new session on every resume attempted after Step 7 has run, losing the prior session's phase, feature, and fix-cycle state. This is a known limitation of the skill's identity mechanism, not something this SOP works around. Treat any resume of a greenfield project past Step 7 as a fresh start, and warn the engineer of this if a greenfield resume is attempted.
- **Phase list.** This SOP's phases, in order, are: `elicitation` (Intake Elicitation), `codebase-analysis` (Codebase Analysis), `requirements` (Requirements), `design` (Design), `design-review` (Principal Engineer Design Review), `feature-splitting` (Feature Splitting), `specs` (Specs (Per Feature)), `implementation` (Implementation), `code-review` (Code Review and Pull Request), `testing` (Testing), `documentation` (Documentation), and `final-summary` (Final Summary), the same ids as Steps 0 through 11 and the `skip_phases` parameter, plus `final-summary`, which has no `skip_phases` token of its own because Step 11 cannot be skipped. Resolve this before Step 0 ever runs, via the skill's Resume Discovery operation (per the Session identity bullet above): if it resumes an existing session, adopt its returned `phases` map as-is rather than re-minting it; only if it finds no matching candidate do you then mint the session with this full phase list and this run's `skip_phases` as the skip set via the skill's Mint Session operation, marking each `skip_phases` phase `SKIPPED` up front the same way the checklist below does.
- **The checklist and the skill's `phases` map coexist by design. One does not replace the other.** `sdlc-checklist.md` remains the human-readable record you display at every engineer pause: phase status, feature status, artifact paths, and free-text detail a fixed schema cannot hold, such as a re-derived status, an unverified completion, a named blocking reason. The skill's `phases.<phase-id>` entries are the structured counterpart: what a resumed session re-parses to know where it left off without re-reading markdown prose, the same reasoning that already keeps `fix_cycles_used` and `e2e_project_dir` out of the checklist. You MUST update both on every phase-status change, through the skill's Write Safety order (state file first, checklist second), never one without the other, and never as a single batched or parallel dispatch: per the skill's own sequential-dispatch requirement, you MUST NOT hand off the checklist write until the delegated state-file write has returned confirmed success. You MUST display the checklist at every point where you pause for engineer input, including the attended-mode Resume Discovery pause; see that operation for the one case (before the checklist is first created) where the engineer is asked directly instead.
- **Per-feature phase completeness (Steps 6 through 9).** The skill's Read/Write a Phase Entry operation has no opinion on when a per-feature phase is done. That judgment belongs here. For `specs`, `implementation`, `code-review`, and `testing`, You MUST track per-feature state on the checklist, not just per-phase, and call the skill's phase-write operation for the phase itself only once you have decided: `IN_PROGRESS` from the first feature entering the phase, `COMPLETED` with a summary once every feature has cleared it, and `BLOCKED` only if the phase itself is stopped for every remaining feature. A single feature left BLOCKED while others continue leaves the phase-level entry `IN_PROGRESS`. The checklist's per-feature detail is where that one feature's block is visible.
- **Fix-cycle counters.** Steps 7, 8, and 9 draw from one counter per feature, named `fix_cycles_used`, via the skill's Register a Feature and Read/Increment a Counter operations. Each feature MUST be registered as soon as it is known: Step 5 does this immediately after `task-decomposition` returns its output, or, when `feature-splitting` is in `skip_phases`, whichever step first reads the externally-supplied `feature-split.md` does it in Step 5's place, per the pre-existing-artifact check in Parameters above. Step 10 draws from its own run-level counter, `doc_fix_cycles_used`, since its documentation validation is project-wide, not per-feature. There is no feature slug to attach it to. Both draw on the same `max_fix_cycles` limit but are otherwise independent. Because Steps 7, 8, and 9 MAY run up to 4 features in parallel under `worktree` isolation, You MUST follow the skill's concurrency rule for every write against the session file, not `fix_cycles_used` alone: hold the skill's mutual-exclusion primitive (see Read/Increment a Counter's Concurrency scope) across each feature's full read-increment-write cycle, so no two features' writes, whether counter, phase, or opaque-data, ever overlap on the shared session file. Two features' cycles MUST NOT interleave between a read and its matching write. A feature blocked on another feature's held lock MUST follow the skill's stale-lock recovery rule rather than retry without limit: past the fixed 30-second threshold the lock is treated as abandoned, since the holding feature's process is assumed to have crashed mid-cycle rather than merely run long, and stolen, applying the skill's TOCTOU-safe mtime re-check immediately before removal.
- **Other run- and feature-level fields.** Store `feature_isolation` (this run's parameter, written once at minting and never overwritten by a later invocation's parameter value) and `e2e_project_dir` (run-level under `branch` isolation, per-feature under `worktree` isolation; read `feature_isolation` first to know which slot is authoritative) via the skill's Read/Write Opaque Data operation.
  - **Reconciliation on resume (feature_isolation).** Whether resumed automatically or by an explicitly named file, if this run's `feature_isolation` parameter differs from the resumed session's stored value, You MUST NOT adopt either silently. This is a hard stop for the whole run: mark the run BLOCKED on the checklist, name both values and the resumed session's file path, and stop pending the engineer's explicit choice: either re-invoke with `feature_isolation` matching the stored value to continue this session under its original strategy, or start a fresh session (a new or distinct `output_dir`) to use a different strategy for what is, at that point, new work rather than a continuation. A brand-new session has no stored value to conflict with. It simply records this run's `feature_isolation` parameter as its own, via the skill's Read/Write Opaque Data operation immediately after minting (Mint Session itself has no slot for this field; it initializes `run.data` empty). This check happens once, here, at resume. Every branch or worktree operation in Step 7, for every feature, at any point in this or a later resume of the same session, MUST use the value the session actually stored, never a value re-read from a later invocation's own parameter.
- **Ownership.** You alone invoke the skill's operations and own the checklist. Spawned agents MUST report their status back to you rather than writing either directly, so parallel feature work cannot corrupt them. You MUST carry each phase's artifact paths on the checklist as they are produced. Step 11 reads from it. You MUST delegate the actual file writes, both the skill-owned session file and `sdlc-checklist.md`, to a subagent (`k-developer`), passing the exact content to write, the same as every other artifact this SOP produces: you alone decide what and when to write; the subagent performs the write. This keeps checklist and state ownership with you without requiring you to hold a standing file-write tool grant.
- **Pre-step reconciliation gate (every step, not only at resume).** Before delegating any step's own work, that is, Steps 0 through 11, on every pass through this SOP, You MUST read this session's `phases` map via the skill's Read/Write a Phase Entry operation and compare it against `sdlc-checklist.md`'s current phase status. For Steps 6 through 9, You MUST additionally confirm every feature slug on the checklist still has a corresponding entry in this session's `features` map. The `phases` map itself carries no per-feature slot. A feature's completion status is never duplicated into session state; it lives on the checklist alone, re-derived from disk when it needs correcting (see the Post-resume consistency check below). So the only thing session state can disagree with the checklist about, for a feature, is whether that feature is registered at all, never what phase it has reached. Skip this gate on the first step of a freshly minted session, where no checklist yet exists. The fixed Write Safety order (state file first, checklist second) makes only one direction of disagreement a legitimate crash outcome; handle the two directions differently:
  - **State file ahead of the checklist.** The `phases` map already shows a phase done while the checklist still shows `PENDING` or `IN_PROGRESS`. This is what a crash between the state-file write and its paired checklist write produces. You MUST update the checklist to match the `phases` map, per-feature detail via the expected-artifact re-derivation in the Post-resume consistency check bullet below, then re-display the reconciled checklist, before proceeding with this step's delegation. Leave the state file itself untouched. This direction has no feature-level counterpart: the `features` map carries no completion status for a checklist entry to lag behind.
  - **Checklist ahead of the state file.** For a phase, the checklist shows it done while the `phases` map still shows `PENDING` or `IN_PROGRESS`. The fixed write order rules this outcome out under normal operation, and because the `phases` map already has every phase ID present at minting (see Progress Tracking's Phase list bullet below), this can only ever be a status disagreement between two existing entries, never an entry missing outright. Handle it the same way the Post-resume consistency check's re-derivation bullet handles the equivalent per-feature case: re-derive the phase's actual status from disk against its own expected artifact set (this phase's Expected Output in the Steps section below; for a per-feature phase, Steps 6 through 9, re-derive every feature's own status first, per that same bullet, then apply Progress Tracking's per-feature completeness rule to the phase as a whole), correct the checklist to match, then proceed with this step's delegation. Do not stop or wait for the engineer. For a feature (Steps 6 through 9 only), the checklist lists a slug this session's `features` map is missing outright. This is the actual shape the Post-resume consistency check's BLOCK-and-surface bullet handles, since a `features` entry exists only once something has explicitly registered it. Apply that handling here too: mark the feature BLOCKED with the disagreement named, and stop for that feature pending the engineer, rather than writing either file.

  This gate is separate from, and applies in addition to, the post-resume check below, which runs once, immediately after a resume; this gate runs before every step of the run, resumed or not.

- **Feature-registration gate (before Step 6).** Before Step 6 begins, for every feature slug recorded on the checklist from Step 5's `{Feature Name} → {feature-name}` pairs, You MUST confirm the session's `features` map already carries an entry for that slug. You MUST register any missing slug now, through the skill's Register a Feature operation, before Step 6's first `kiro-spec-workflow` invocation. **This gate's scope excludes a feature slug the checklist already marks BLOCKED with "session state entry missing for a feature the checklist already reports on"**. That entry stays exactly as the pre-step reconciliation gate or the post-resume consistency check left it, unregistered and BLOCKED, until the engineer supplies its `fix_cycles_used` value or explicitly accepts starting it fresh at `0`; this gate MUST NOT register it in the engineer's place. A feature left unregistered outside that exception has no `fix_cycles_used` counter for Steps 7 through 9 to read from.

- **Post-resume consistency check.** Run this only after the feature_isolation reconciliation check above has already passed without stopping the run. A feature_isolation mismatch is a hard stop for the whole run, before any feature-level state is even relevant, so it takes precedence over this per-feature comparison, not the reverse. Once past that gate, immediately after the skill's Resume Discovery operation resumes a session, and before doing any further phase work, for every feature slug, compare (a) whether it is present in the resumed `features` map versus the checklist, and (b) where present in both, whether the checklist's recorded phase status for it agrees with what actually exists on disk. Comparing only the _set_ of slugs is not enough. A feature present in both, whose recorded status disagrees with the artifacts after a crash mid-transition (the checklist still shows the previous phase, or already shows the next one, while the artifacts on disk tell a different story), would otherwise go undetected.
  - **Re-deriving a phase status from disk requires the phase's full expected-artifact set, not any single artifact.** Each phase's expected set is fixed: `specs/` is only complete once `requirements.md`, `design.md`, AND `tasks.md` all exist (`kiro-spec-workflow` writes these three in sequence, so a crash after the first one or two leaves `specs/` non-empty but incomplete); `code-review/` is only complete once both `report.md` AND `status.md` exist; `testing/` is only complete once `k-test-coverage-review`'s own `test-gap-analysis.md`, `e2e-test-strategy.md`, AND `test-coverage-review-report.md` all exist (this is the same principle Step 9 already applies when distinguishing a real testing pass from a Kiro-stub `tasks.md`-only directory). A subtree that has artifacts present but is missing any one file from its phase's expected set has NOT completed that phase. The correct re-derived status is the deepest phase whose full expected set is present, one phase earlier, never the partially-populated one.
  - **Implementation (Step 7) cannot be re-derived from `output_dir` by this expected-artifact-set method at all.** Unlike `specs/`, `code-review/`, and `testing/`, Implementation writes to the project tree, not under `output_dir`. There is no `output_dir/{feature-name}/implementation/` subtree for this mechanism to inspect, by design (see Artifact Layout: "Implementation code, tests, and build configuration go where the project puts them"). Its only observable signal from inside `output_dir` is the checkbox state of `output_dir/specs/{feature-name}/tasks.md`, already inspectable since `specs/` is under `output_dir`, plus whatever the project's own git state shows outside it. Apply this rule where `specs/` is re-derived as complete but `code-review/` is not (code review presupposes implementation, so a complete `code-review/` subtree already implies Implementation finished; this rule only matters in the gap between the two): if any task in `tasks.md` is unchecked, Implementation is definitely not complete for that feature. Its re-derived status stays at the deepest phase actually complete, i.e. specs COMPLETED, implementation not yet done. If every task in `tasks.md` is checked, that is necessary but not sufficient evidence. A crash could land after the last checkbox was checked off but before Step 7's own build-and-test-after-each-feature requirement confirmed the build actually passed, or the code could have been reverted since. You MUST NOT mark Implementation COMPLETED on checkbox state alone. Doing so risks silently reporting an incomplete or broken Implementation as done. Instead, record that feature's Implementation as `COMPLETED (unverified — tasks.md fully checked, pending confirmation)` and mark the feature BLOCKED on the checklist pending either the engineer's explicit confirmation or a fresh re-run of the build and test commands recorded on the checklist in Step 7 passing clean; only one of those two, never the checkbox state by itself, may promote it to a bare `COMPLETED`.
  - A feature slug present in the resumed `features` map but missing from the checklist, or present in both but with a checklist-recorded status that disagrees with what the expected-artifact re-derivation above says the deepest completed phase actually is, means the checklist write either did not happen or is stale relative to what is really on disk (a crash between finishing one phase's writes and the paired checklist write, consistent with the skill's fixed write order; or a crash mid-transition into the next phase after the checklist had already recorded the previous one). You MUST re-derive that feature's status per the expected-artifact rule above, add or correct it on the checklist, and mark it distinguishably from a normally-reported status, e.g. `COMPLETED (re-derived)` rather than a bare `COMPLETED`, so the engineer can tell a status this SOP itself observed from one it reconstructed after a crash. This corrected value is per-feature detail; it lives on the checklist only. The skill's `phases.<phase-id>` entry has no per-feature slot to write it into. Separately, re-apply Progress Tracking's per-feature completeness rule to the phase as a whole now that this feature's status is corrected: if every feature has, in fact, cleared the phase (this correction included), call the skill's phase-write operation to set that phase's aggregate entry to `COMPLETED` with a summary; otherwise leave the phase-level entry as it already stands. Then write the corrected checklist back, before proceeding.
  - A feature slug present on the checklist but missing from the resumed `features` map means the checklist is ahead of the state file, which the skill's fixed write order says should not happen. You MUST NOT invent a `fix_cycles_used` value for it. Reconstructing one from nothing risks silently resetting or fabricating the fix-cycle budget, exactly the risk this mechanism exists to prevent. Instead mark that feature BLOCKED on the checklist with "session state entry missing for a feature the checklist already reports on" and stop for that feature, surfacing it to the engineer, rather than guessing. **To un-block this case:** the engineer either supplies the feature's correct `fix_cycles_used` value (from their own record of how many fix cycles it actually consumed, or `0` if none) for you to register via the skill before resuming work on it, or accepts starting that feature's budget fresh at `0`. Either way, the un-block is an explicit engineer decision recorded on the checklist next to the BLOCKED entry it resolves, never a value this SOP guesses on its own.
  - The skill's Resume Discovery operation already skips any malformed or sentinel-fingerprint-collision candidate it encounters along the way and reports which paths it skipped. Record those paths on the checklist so the engineer can inspect them later. This check assumes the file it is comparing against already parsed successfully; a parse failure is the skill's concern, not this check's.

## Artifact Layout

```
.konductor/ default output_dir
├── codebase-analysis/analysis.md
├── requirements/                    business-context.md, requirements-summary.md, user-stories-raw.md, user-stories-extract.md
├── design/                          system-design.md, principal-engineer-review.md
├── feature-split/feature-split.md
├── specs/{feature-name}/            requirements.md, design.md, tasks.md — one subtree per feature from Step 6
├── {feature-name}/                  one subtree per feature from Steps 8 and 9
│   ├── code-review/                 report.md, status.md
│   └── testing/                     coverage and test-strategy reports, ui-prompts/
├── sdlc-checklist.md
├── sdlc-summary.md
└── sessions/
    └── k-full-sdlc-<timestamp>-<suffix>.yml   one file per session — see Progress Tracking and the `sop-state-management` skill
```

Steps 1 through 5 are project-wide and write directly under `output_dir`. Step 6 is per-feature and writes under `output_dir/specs/{feature-name}/`, matching `kiro-spec-workflow`'s own convention of nesting the feature name under `specs/` rather than the other way around. Steps 8 and 9 are per-feature and write under `output_dir/{feature-name}/`, so a feature's code-review and testing artifacts sit together in one subtree, separate from that same feature's specs, which is also what keeps parallel features' code-review and testing artifacts from overwriting each other, since the delegated SOPs write fixed filenames within that subtree. Step 7 is per-feature too, but its only touch under `output_dir` is checking off tasks in the `specs/{feature-name}/tasks.md` Step 6 already created. Implementation code itself goes into the project tree, never under `output_dir/{feature-name}/`. `output_dir` holds only SDLC process records: specs, sessions, requirements, design docs, the checklist. Step 10's project deliverables (`README.md`, `developer-guide.md`) ship with the code, so they are never written under `output_dir` at all. They go directly to the project root (`codebase_path` on a brownfield project, the source directory recorded on the checklist in Step 7 on a greenfield project), alongside every other exception that escapes `output_dir` by design: Step 3's `docs/design/` reports (project-wide, named from a slug of the design topic rather than a fixed name), Step 9's `k-e2e-test-generation` pass in `add-to-existing` mode, which writes generated test specs into the project's own existing test directory rather than under `output_dir/{feature-name}/testing/`, and Step 10's repository-root `AGENTS.md` (produced only if the repo lacks one). `sdlc-summary.md` is otherwise the only project-wide artifact Step 11 adds under `output_dir`.

**Known limitation: `output_dir` is fixed, not session-scoped.** Only `sessions/` is per-session. Each run's state file keeps its own timestamped name. Everything else under `output_dir` uses fixed filenames, so a second run against the same `output_dir` overwrites the first run's artifacts: `requirements/`, `design/`, `feature-split/`, every `specs/{feature-name}/` subtree, every `{feature-name}/` subtree, `sdlc-summary.md`, and `sdlc-checklist.md` all get replaced. This is accepted, not a defect. An engineer who wants to keep a completed run's artifacts must copy them out before starting the next run, or pass a distinct `output_dir`. `README.md` and `developer-guide.md` are unaffected by this. They live at the project root (`codebase_path` on a brownfield project, the Step 7 source directory on a greenfield project), not under `output_dir`, so a second run against the same `output_dir` overwrites them only if it also targets the same project root. Nothing in either mode pauses specifically to flag an impending overwrite before it happens. An attended engineer relying on the shared default `output_dir` is trusting that no other run has used it, not being warned in the moment this run replaces its content. The explicit, non-default `output_dir` required of an unattended run (see that parameter's own entry above) exists because an unattended run additionally has no engineer to ask at all, not because an attended run has some other real-time protection against the same overwrite.

`sdlc-summary.md` is the run's only rollup: Step 11 reads the per-feature status files and reports phase statuses, every artifact path, and everything left open.

`output_dir` holds only the markdown this SOP produces. Implementation code, tests, and build configuration go where the project puts them. Step 7 writes into the project tree and tracks its progress in `specs/{feature-name}/tasks.md`, so it adds nothing here.

Create a directory only when a step writes into it. A skipped phase leaves no empty directory behind.

One set of artifacts lands outside `output_dir`: `k-design-doc-creation` writes its validation and trade-off reports into `docs/design/` by default, using names derived from the topic it was given. That SOP now accepts an optional `validation_output_dir` parameter to redirect them, but this SOP does not pass it. The Step 3 subagent instead reports the two paths it actually wrote onto the checklist, and Step 11 lists them from there.

## Steps

### 0. Intake Elicitation

Use the `socratic-elicitation` skill (Mode A: Intake) to resolve an ambiguous or under-specified `project_description`.

**Constraints:**

- You MUST skip this step if `elicitation` is in `skip_phases`, or if `project_description` is already clearly scoped, or if the engineer signals "skip" / "just do it".
- You MUST configure the skill with the `elicitation_depth` parameter and the stakeholder tags `[PM]`, `[SDE]`, `[Security]`, `[Ops]`, `[TPM]`.
- You MUST ask questions sequentially, one at a time, and wait for the skill's Requirements Summary before proceeding.
- If `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark this step BLOCKED on the checklist, naming the ambiguity that triggered elicitation, and stop rather than waiting indefinitely for an answer, the same handling as the `project_description` gate above.
- Record the `elicitation` phase's outcome via the `sop-state-management` skill at the same point you update the checklist above: `SKIPPED` with the skip reason if this step is skipped, `COMPLETED` with a one-line summary of the Requirements Summary otherwise, `BLOCKED` if it stops per the bullet above.

**Expected Output:** Requirements Summary, carried into Steps 1 and 2 as context.

### 1. Codebase Analysis

Dispatch the agent the `delegation-protocol` skill's Per-Agent SOP Registrations table lists for `k-codebase-analysis` to run that SOP, understanding the existing code before designing against it.

**Constraints:**

- You MUST skip this step if `codebase-analysis` is in `skip_phases`, or if this is a greenfield project with no existing code.
- You MUST set `codebase_path` to the parameter of the same name and `output_file` to `output_dir/codebase-analysis/analysis.md`.
- You MUST carry its findings into Steps 2 and 3. Requirements must not contradict what exists, and the design must state how it fits the current architecture.
- If it reports architectural constraints that make `project_description` infeasible as written, You MUST surface them and wait before proceeding. If `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark this step BLOCKED on the checklist with the conflicting constraints named and stop, the same handling as Step 0's `project_description` gate.
- Record the `codebase-analysis` phase's outcome via the `sop-state-management` skill at the same point you update the checklist: `SKIPPED` with the reason (e.g. "greenfield project, no existing code") if this step skips itself, `COMPLETED` with a one-line summary of the analysis otherwise, `BLOCKED` if it stops per the bullet above.

**Expected Output:** `output_dir/codebase-analysis/analysis.md`.

### 2. Requirements

Spawn `k-product-manager` to produce requirements artifacts.

**Constraints:**

- You MUST skip this step if `requirements` is in `skip_phases`.
- You MUST instruct it to write user stories with `user-story-writing`, then extract design inputs with `requirements-extraction`.
- Neither skill writes to disk on its own. `user-story-writing` returns Epics/Stories as chat output only, and `requirements-extraction`'s SKILL.md names illustrative example paths (`requirements-planning/outputs/*.md`) that do not apply here. You MUST have the subagent (1) write `user-story-writing`'s raw output to `output_dir/requirements/user-stories-raw.md` itself, not `user-stories-extract.md`, which is `requirements-extraction`'s own output filename and would be overwritten by step (2) if both used it, then (2) instruct `requirements-extraction` to read that raw file, not its documented example paths, and write all three of its own files, including `user-stories-extract.md`, to `output_dir/requirements/`.
- You MUST NOT proceed until every one of the following holds. If any fails, present the gaps and wait. If `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark this step BLOCKED on the checklist and stop rather than proceeding on unresolved requirements. Step 3 and every step after it depends on this artifact being complete:
  - Every user story has explicit acceptance criteria.
  - No Open Item from Step 0's Requirements Summary is unresolved.
  - Nothing contradicts Step 1's analysis.
- Record the `requirements` phase's outcome via the `sop-state-management` skill at the same point you update the checklist: `COMPLETED` with a one-line summary once every bullet above holds, `BLOCKED` otherwise.

**Expected Output:** `business-context.md`, `requirements-summary.md`, `user-stories-raw.md`, `user-stories-extract.md` in `output_dir/requirements/`.

### 3. Design

Dispatch the agent the `delegation-protocol` skill's Per-Agent SOP Registrations table lists for `k-design-doc-creation` to run that SOP and produce the design document.

**Constraints:**

- You MUST skip this step if `design` is in `skip_phases`.
- You MUST set `topic` to the requirements from Step 2, and `output_path` to `output_dir/design/system-design.md`.
- You MUST always pass the literal phrase `skip elicitation`. That SOP's Phase 1 matches on it. Either Step 0 already asked those questions, or it was skipped because the engineer declined them, the description was already clear, or `elicitation` is in `skip_phases`. In every one of those cases a second round of up to 10 questions is unwanted.
- If Step 1 ran, You MUST pass its analysis so the design accounts for the existing architecture. If it did not, the design MUST name the language, framework, and runtime. Step 7 scaffolds from that choice and has nothing else to go on.
- If Step 1 did not run, You MUST confirm the finished design names all three before leaving this step. If any is missing, You MUST NOT proceed to Step 4. Return to `k-design-doc-creation` with the gap named, or ask the engineer to choose the stack. If `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark this step BLOCKED on the checklist with the missing item named and stop, the same handling as Step 0's `project_description` gate.
- That SOP owns its own quality gates and adversarial loop. You MUST NOT re-run them here.
- You MUST check that SOP's own reported exit state before marking this step COMPLETED on the checklist. Its adversarial loop can itself end early, blocked on an unresolved CRITICAL, an engineer override, or a stopped run, and a subagent report of "done" does not by itself confirm which. If it did not reach its own completed state, You MUST mark this step BLOCKED with that reason rather than COMPLETED, and MUST NOT proceed to Step 4 with a design document that SOP itself does not consider finished.
- Record the `design` phase's outcome via the `sop-state-management` skill at the same point you update the checklist: `COMPLETED` with a one-line summary only when the bullet above confirms that SOP's own completed state, `BLOCKED` otherwise.

**Expected Output:** `output_dir/design/system-design.md`. That SOP also writes validation and trade-off reports to `docs/design/` by default (it now accepts an optional `validation_output_dir` override, which this SOP does not use), naming them from a slug of the `topic` it was given, which this SOP cannot predict. You MUST have the Step 3 subagent report back the two paths it actually wrote and record them on the checklist, so Step 11 lists real paths instead of guessing.

### 4. Principal Engineer Design Review

Dispatch the agent the `delegation-protocol` skill's Per-Agent SOP Registrations table lists for `k-principal-engineer-design-review` to run that SOP against the design document.

**Constraints:**

- You MUST skip this step if `design-review` is in `skip_phases`.
- You MUST set `doc_input` to `output_dir/design/system-design.md` and `output_file` to `output_dir/design/principal-engineer-review.md`.
- Step 3's own adversarial loop challenged the draft; it does not substitute for this gate. This step is an independent second reviewer applying the same tooling (`design-quality-check`, `argumentation-reference`) from a fresh, non-authoring perspective, which is what catches what the document's own author-and-critic loop missed, not different tooling doing a different check.
- If the review returns CRITICAL findings, You MUST present them and MUST NOT proceed until the engineer resolves or overrides them. If `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark this step BLOCKED on the checklist with the CRITICAL findings recorded and stop, the same handling as Step 0's `project_description` gate.
- Route design fixes back to Step 3, not to implementation.
- Record the `design-review` phase's outcome via the `sop-state-management` skill at the same point you update the checklist: `BLOCKED` while CRITICAL findings remain unresolved, `COMPLETED` with a one-line summary once the engineer confirms.

**Expected Output:** `output_dir/design/principal-engineer-review.md` and engineer confirmation to proceed.

### 5. Feature Splitting

Spawn `k-developer` to split the work into independently buildable features.

**Constraints:**

- You MUST skip this step if `feature-splitting` is in `skip_phases`.
- You MUST instruct it to use `task-decomposition` with `output_dir/requirements/user-stories-raw.md` and `output_dir/design/system-design.md` as input, the raw file, not `user-stories-extract.md`. The extract keeps only 3-5 architecturally significant stories per epic for the design interview; splitting features from it would drop every story it didn't select, and those stories would never appear in any feature's scope.
- Each feature MUST be buildable independently, with minimal overlap between features.
- You MUST derive a kebab-case slug for every feature immediately after `task-decomposition` returns its output: lowercase, spaces and `&`/`/` replaced with `-`, stripped of any character outside `[a-z0-9-]`. Two feature names differing only in case, punctuation, or whitespace can derive the same slug, which would silently merge their `output_dir/{feature-name}/` subtrees (and, under `worktree` isolation, their branch and worktree names too), so You MUST assign final slugs with this algorithm, not by rechecking each candidate against a single fixed set computed once up front:
  1. Maintain one running set, `assigned-slugs`, empty at the start.
  2. Process features one at a time, in the order `task-decomposition` returned them. This order is fixed for the whole pass. Never re-sort or re-batch by collision.
  3. For each feature: if its derived slug is not already in `assigned-slugs`, that derived slug is its final slug. If its derived slug IS already in `assigned-slugs`, because an earlier feature in this same pass claimed it, or because another feature's own free-text name already normalizes to it (e.g. a feature literally named "X 2" alongside a feature named "X"), try `{slug}-2`, then `{slug}-3`, `{slug}-4`, and so on, checking each candidate against `assigned-slugs`, until reaching a candidate not in the set. That candidate is this feature's final slug.
  4. Immediately add the feature's final slug to `assigned-slugs` before moving to the next feature. This is what makes the check accumulate: a later feature's candidate is checked against every slug already claimed in this pass, not against the original pre-disambiguation set, so two features colliding on the same base slug can never both be assigned the same disambiguated candidate.
  5. Record on the checklist every feature whose final slug differs from its bare derived slug.

  Only once every feature has been processed, and every final slug is therefore already unique by construction, record the final `{Feature Name} → {feature-name}` pairs on the checklist. Every subsequent step MUST use this recorded slug, never the free-text feature name, as `{feature-name}` in any path or branch name.

- You MUST write the split to `output_dir/feature-split/feature-split.md` and present it for approval before proceeding. If `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark this step BLOCKED on the checklist with the proposed split recorded and stop, the same handling as Step 0's `project_description` gate.
- Record the `feature-splitting` phase's outcome via the `sop-state-management` skill at the same point you update the checklist: `COMPLETED` with a one-line summary once the engineer approves the split, `BLOCKED` otherwise. Also register each feature (per its slug from the bullet above) with the skill's Register a Feature operation as soon as its final slug is assigned, so Steps 6 through 9 have a counter to read from.

**Expected Output:** `output_dir/feature-split/feature-split.md` listing each feature's scope and dependencies.

### 6. Specs (Per Feature)

Invoke `kiro-spec-workflow` once per feature. It is registered directly on the orchestrator rather than on a single specialist agent, and delegates internally to its own agents (see Execution Rules). Its `requirements.md` / `design.md` / `tasks.md` output drives Steps 7 through 9.

**Constraints:**

- You MUST skip this step if `specs` is in `skip_phases`.
- This step MUST run sequentially across features even under `worktree` isolation. The engineer approves every feature's specs in one pass at the end, so parallelism would buy nothing.
- For each feature, You MUST set `feature_name` to the kebab-case slug recorded on the checklist in Step 5, not re-derived here, `feature_scope_path` to `output_dir/feature-split/feature-split.md`, `user_stories_path` to `output_dir/requirements/user-stories-raw.md` (that SOP's own Step 1 filters this to the stories mapped to the current feature; the curated `user-stories-extract.md` has already dropped the stories not selected for the design interview, so filtering it can only ever produce a subset of a subset), and `design_artifacts_path` to `output_dir/design/`.
- You MUST set that SOP's own `spec_dir` to `output_dir/specs/{feature-name}/`. Its default is `.kiro/specs/{feature_name}/`, which would land the specs outside this run's `output_dir`. Override it so every artifact stays in one place, keeping the same `specs/` above `{feature-name}` nesting that default uses.
- You MUST NOT proceed until the engineer approves the spec documents for every feature. If `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark this step BLOCKED on the checklist for every feature awaiting approval and stop, the same handling as Step 0's `project_description` gate.
- Record the `specs` phase's outcome via the `sop-state-management` skill only once every feature's specs are approved, per Progress Tracking's per-feature completeness rule, `COMPLETED` with a one-line summary once all are, not per feature as each one clears.

**Expected Output:** `requirements.md`, `design.md`, `tasks.md` per feature under `output_dir/specs/{feature-name}/`.

### 7. Implementation

Spawn `k-developer` once per feature to implement its tasks.

**Constraints:**

- You MUST skip this step if `implementation` is in `skip_phases`.
- You MUST pass the feature's `requirements.md`, `design.md`, and `tasks.md` as context, and instruct it to work through `output_dir/specs/{feature-name}/tasks.md` task by task using `backend-development` and/or `frontend-development`, checking off each task there as it completes. That file is the task plan. Do not create a second one. Code, tests, and build configuration go where the project's own structure puts them, not under `output_dir`.
- On a greenfield project, the first task You MUST complete is the scaffold: an initialized git repository, the language and framework chosen in Step 3's design, build configuration, dependency manifest, test framework, and directory layout. You MUST choose the scaffold's target directory before creating it, a new directory under `codebase_path` (creating `codebase_path` itself first if it does not yet exist) named from the project, unless the engineer specified one, and record that path on the checklist as the source directory alongside the build command, the test command, and the test directory. It MUST build and run an empty test suite successfully before any feature code is written. Steps 8, 9, and 10 all read these four from the checklist. On a brownfield project, take those four from Step 1's analysis instead: its `## Codebase Map` section records a build-system tool-name label (e.g. `npm`, `cargo`, `make`) and entry points, not four ready-to-run strings. Infer the build and test commands from that label using the ecosystem's own convention (e.g. `npm` → `npm run build` / `npm test`; `cargo` → `cargo build` / `cargo test`; `make` → `make build` / `make test`, adjusted to the actual target names present in the manifest Step 1 identified), and take the source and test directories from the top-level directory structure Step 1's Codebase Map records. If the inferred build or test command fails when you run it, that failure is itself the signal Step 1's analysis was insufficient. Do not retry the same inference. Stop and ask the engineer for the correct build command, test command, source directory, and test directory instead. If Step 1 was skipped (`skip_phases` includes `codebase-analysis`) and this is a brownfield project, there is nothing to infer from. You MUST NOT guess. Stop and ask the engineer for the build command, the test command, the source directory, and the test directory before proceeding. In either case, if `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark this step BLOCKED on the checklist with the missing or failing values named and stop, the same handling as Step 0's `project_description` gate.
- You MUST NOT invoke `backend-development` or `frontend-development` for any feature task until the source directory and test directory are recorded on the checklist. Those skills have no location contract of their own and rely entirely on this SOP's checklist state to know where to write.
- Feature code lives in the project, never under `output_dir`: on a brownfield project inside `codebase_path`, on a greenfield project inside the source directory recorded on the checklist above. All features share that one source tree. Step 8 opens a pull request per feature off a branch, which only works if they do. This holds under either `feature_isolation` mode: a `worktree` is another checkout of the project repository (`codebase_path` on a brownfield project, the Step 7 source directory on a greenfield project), not a separate copy elsewhere, so a feature's code is already part of the project the moment it is committed to its branch. It joins the mainline branch only once the engineer merges the reviewed pull request. See Step 8's prohibition on this SOP merging any branch itself.
- Before implementing, You MUST set up isolation per `feature_isolation`, specifically the session's stored `feature_isolation` value (Progress Tracking's reconciliation-on-resume rule), not a value re-read from this invocation's own parameter, since the two are only guaranteed to agree once that reconciliation check has passed:
  - `branch`: create one branch per feature in the current working directory, and implement features one at a time.
  - `worktree`: create one `git worktree` per feature so features can be implemented in parallel.
- Before creating the first branch or worktree, You MUST confirm the working tree is clean. If the engineer has work in progress, stop and ask. Otherwise it lands in the first feature's pull request. If `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark this step BLOCKED on the checklist with the uncommitted work named and stop, the same handling as Step 0's `project_description` gate.
- You MUST commit each feature on its own branch at the end of that feature's work, before starting the next one. Step 8 runs after every feature is implemented, so leaving changes uncommitted here means switching branches either drags one feature's changes onto another's or refuses outright, and Step 8 then commits a mixture onto whichever branch is current.
- If there are two or more features and `feature_isolation` is `branch`, You MUST offer worktrees once: state that worktrees would allow the features to be built in parallel, name the directory where they would be created, and ask for confirmation. Proceed with `branch` unless the engineer agrees. If `reply_channel_available` is `false` (an unattended run with no reply channel), treat the absence of a reply as declining the offer and proceed with `branch`. This offer is equivalent to a no in that context, not a blocking wait.
- You MUST NOT create a worktree without explicit confirmation. It writes new directories outside the current tree. An explicit `feature_isolation=worktree` passed at intake IS that confirmation; the engineer already chose it, so do not ask again.
- After a feature's pull request is opened in Step 8, and that feature has either cleared Step 9 or Step 9 was skipped, You MAY remove its worktree, but only after confirming the branch is pushed, the worktree has no uncommitted changes, and no fix cycle is currently in flight for this feature. Steps 8 and 9 both read `source_dir` from inside the worktree, so removing it while either still needs it destroys the tree they are reading. You MUST perform the clean-check and the removal as one uninterrupted sequence, with no intervening operation between them. A fix cycle that routes back to this feature between the check and the removal can reintroduce uncommitted work into a worktree you are about to delete. You MUST NOT remove a worktree holding uncommitted work.
- If a fix routes back to this step for a feature whose worktree was already removed, You MUST re-provision the worktree from its pushed branch before proceeding. Do not assume it still exists. If `git worktree add` fails because the prior removal was partial (a stale `.git/worktrees/` entry or an occupied directory path), You MUST retry that operation up to 5 times with a short backoff (1s, 2s, 4s, 8s, 16s) rather than fail the feature immediately, the same as the ref-lock case below. If the failure persists after 5 retries, You MUST stop that feature and surface the error to the engineer.
- Worktrees share one `.git` ref store, so concurrent branch creation, commit, and push across parallel features can fail on a ref lock. On a lock error You MUST retry that operation up to 5 times with a short backoff (1s, 2s, 4s, 8s, 16s) rather than fail the feature immediately. If the lock persists after 5 retries, You MUST stop that feature and surface the ref-lock error to the engineer.
- You MUST write unit tests alongside the code, in the project's test framework and layout. A task is not done until its tests exist and pass.
- Where a task crosses a component boundary, such as a service call, a database write, or a queue, You MUST add an integration test for that boundary.
- You MUST run the build and test commands after each feature. On failure, You MUST read the feature's `fix_cycles_used` counter via the `sop-state-management` skill, spawn `k-developer` to fix it and re-run, then increment and write back the counter through that skill, up to `max_fix_cycles` from this run's shared per-feature budget, the same counter Steps 8 and 9 also read and increment, per the `max_fix_cycles` parameter definition above. If the budget is exhausted and the failure persists, You MUST mark this feature BLOCKED on the checklist with the failing build/test output recorded, then stop and surface it to the engineer rather than proceeding with a broken build. A resumed run otherwise has no signal this feature's build was failing, and Step 11 has nothing to read. (Here, and for the rest of Steps 7 through 9, "stop" scopes to this feature only. Other features already in flight continue, and the Quality Gate tolerates a feature left BLOCKED while others complete. Steps 0 through 6 are the only places in this SOP where "stop" means halt the whole run, with one exception: Step 8's consolidated-verdict gate fires only after every feature has finished, so by the time it applies there is no single feature left to scope to. There, "stop" means pause the whole run pending the engineer's response, the same as Steps 0 through 6.)
- Record the `implementation` phase's outcome via the `sop-state-management` skill only once every feature has built and tested clean, per Progress Tracking's per-feature completeness rule. A single feature left BLOCKED while others continue leaves the phase-level entry `IN_PROGRESS`, not `BLOCKED`. The checklist's per-feature detail is where that one feature's block is visible.

**Expected Output:** Implementation code and tests in the project tree, with every task in `output_dir/specs/{feature-name}/tasks.md` checked off.

### 8. Code Review and Pull Request

Commit each feature, open its pull request, then dispatch `k-developer` to run `k-code-review-workflow` against it.

**Constraints:**

- You MUST skip this step if `code-review` is in `skip_phases`.
- If the source directory, build command, or test command are not recorded on the checklist, for example because `implementation` is in `skip_phases`, You MUST NOT guess them. Stop and ask the engineer for the four values Step 7 would otherwise have recorded (source directory, build command, test command, test directory) before proceeding. If `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark this step BLOCKED on the checklist with the missing values named and stop, the same handling as Step 0's `project_description` gate.
- For each feature, You MUST first spawn `k-developer` with the `git-workflow` and `code-review` SKILLS (not the `k-code-review-workflow` SOP; that runs next, in the following bullet) to commit that feature on its own branch and open one pull request for it, not one pull request for all features. Pull request creation lives in the `code-review` skill (`gh pr create`); `git-workflow` has no pull request mechanism.
- Only then You MUST have that same agent run the `k-code-review-workflow` SOP against the feature's diff and pull request, setting `source_dir` to the source directory recorded on the checklist in Step 7, `output_file` to `output_dir/{feature-name}/code-review/report.md`, and `cr_url` to the pull request URL from the previous step. The order matters: that SOP's Step 6 adversarial review degrades to diff-only without `cr_url`, so the pull request MUST exist before the review runs. If the project has no pull request host, there is no pull request URL to pass. Omit `cr_url` rather than treating its absence as a blocker; the diff-only degrade is exactly this case, not a failure.

- You MUST NOT merge any feature branch into the mainline branch, by any means, not `git-merge`, not a raw `git merge`, not a pull request merge, not a direct push to mainline. Merging is the engineer's decision after review. Your responsibility ends at an open pull request.
- You MUST fix CRITICAL findings and re-run, reading and incrementing the feature's `fix_cycles_used` counter via the `sop-state-management` skill for each cycle, up to `max_fix_cycles` from this run's shared per-feature budget, the same counter Steps 7 and 9 also read and increment, per the `max_fix_cycles` parameter definition above.
- Each feature MUST write its own verdict to `output_dir/{feature-name}/code-review/status.md`: pull request URL and APPROVED or BLOCKED. If the project has no pull request host, record the branch name and note that no pull request was opened.
- You MUST process every feature before pausing, then present the consolidated verdicts read from those per-feature status files and wait for confirmation to proceed. Step 11 carries them into the final summary, so do not write a separate rollup file. If `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark this step BLOCKED on the checklist with the consolidated verdicts recorded and stop, the same handling as Step 0's `project_description` gate.
- Record the `code-review` phase's outcome via the `sop-state-management` skill only once every feature's pull request has a verdict, per Progress Tracking's per-feature completeness rule.

**Expected Output:** `output_dir/{feature-name}/code-review/report.md` and `output_dir/{feature-name}/code-review/status.md` per feature, each status naming its pull request URL and verdict.

### 9. Testing

Cover three tiers: unit and integration tests written in Step 7, coverage analysis here, and functional tests for a deployed UI.

**Constraints:**

- You MUST skip this step if `testing` is in `skip_phases`.
- If the source directory or test directory are not recorded on the checklist, for example because `implementation` is in `skip_phases`, apply Step 8's guard above: stop and ask the engineer for the missing values, or, if `reply_channel_available` is `false` (an unattended run with no reply channel), mark this step BLOCKED.
- Before dispatching any feature, You MUST ask the engineer two separate questions: whether to plan E2E tests for whatever gaps the coverage analysis finds, and whether to generate and run UI tests for whatever pages or flows discovery finds. They are independent decisions and the engineer may allow one and decline the other. Those SOPs each hold a post-discovery scope gate that cannot be answered per feature when features run in parallel. Every run would stall at once with no engineer turn available. This step has three distinct engineer-input points, and they are separate, non-overlapping turns: this pre-dispatch scope question, the per-feature CRITICAL-gap choice below, and the parallel-features consolidated exchange below that. Do not merge them into one prompt or assume answering one answers another.
- Pass the first answer to `k-test-coverage-review` as `scope_confirmed=true` if the engineer said yes, or `scope_declined=true` if the engineer said no. Never leave both false, which that SOP's own Step 3 cannot distinguish from not having asked yet, and would ask the identical question again. This pair governs only whether its own Step 3/4 (E2E test strategy planning) proceeds or is skipped without asking again. It does not gate whether `k-test-coverage-review` runs at all. You MUST run `k-test-coverage-review` in every case this step is not itself skipped, because its gap analysis (its Step 2) and its security test plan (its Step 5) are exactly what the CRITICAL-gap gate below and the Quality Gate's "0 CRITICAL gaps" evaluate. Skipping the SOP would let both pass with nothing checked. `k-test-coverage-review`'s own parameter contract requires `scope_confirmed=true` from any unattended or parallel caller, since it has no unattended-decline path for its Step 3 prompt, so if `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST pass `scope_confirmed=true` regardless of the engineer's absence, and record in Step 11 that E2E planning was auto-authorized by the unattended-run constraint, not declined. In an attended run, pass `scope_confirmed=true` on yes or `scope_declined=true` on no; a decline there skips that SOP's own Step 4, not its gap analysis or security test plan.
- You MUST run `k-test-coverage-review` first, setting `source_dir` and `test_dir` to the directories recorded on the checklist in Step 7. Both are required and that SOP will stop and ask if either is missing. Set `stories_file` to `output_dir/requirements/user-stories-raw.md`, not `user-stories-extract.md`, which keeps only 3-5 architecturally significant stories per epic and would silently under-report gaps for every story it dropped, and `design_file` to `output_dir/design/system-design.md`. Pass `k-test-coverage-review`'s own `output_dir` parameter as the literal concatenation `{this run's output_dir}/{feature-name}/testing/`, a distinct value from this SOP's own `output_dir`, not the same string. Do not pass this run's `output_dir` unmodified. Use one directory per feature, because features may run in parallel here and would otherwise overwrite each other's reports. It analyses coverage across unit, integration, and E2E, and plans an E2E strategy. It does not write tests.
- `k-test-coverage-review`'s Step 5 delegates to `security-test-generation`, which auto-detects `tool_type=kiro` from the mere presence of a `.kiro` directory in the project root, with no relation to whether this SOP is itself routing through Kiro. In that mode it writes only a task-list checklist of security domains (`tasks.md`) with no per-category findings. Real test content is deferred to a human clicking "Start task" in the Kiro IDE, which does not happen in this unattended pass. You MUST detect this stub before trusting a "0 findings" count from that skill. `output_dir/{feature-name}/testing/` always also holds `k-test-coverage-review`'s own `test-gap-analysis.md`, `e2e-test-strategy.md`, and `test-coverage-review-report.md`, so it never literally contains only `tasks.md`. Check instead for the absence of the per-category security-test files the non-Kiro branch alone produces: `security-test-overview.md`, `best-practices-reference.md`, and any `[domain]-[layer].md` file (see `security-test-generation/SKILL.md` Stage 8's Kiro vs. non-Kiro branches). If `tasks.md` is present and none of those non-Kiro files are, the pass is a Kiro stub. If so, You MUST NOT count that zero toward "0 CRITICAL gaps". Record the security test pass for that feature as NOT EVALUATED and carry it into Step 11 the same way an accepted gap is carried, requiring the engineer's explicit acceptance before the Quality Gate's testing condition can be satisfied.
- Pass the second answer as `scope_confirmed` to `k-e2e-test-generation` or `k-light-ui-testing`. Unlike the first, declining this one does skip the whole pass. Neither SOP's output feeds a gate, so record it in Step 11 as not exercised. If `reply_channel_available` is `false` (an unattended run with no reply channel), treat the absence of a reply as declining it and skip that pass. This is a scope preference, not a blocking wait, the same handling as Step 7's worktree offer.
- If the feature has a web UI and `deployed_url` is set, You MUST run exactly one of the two UI passes below, never both. Pick `k-e2e-test-generation` when the UI is stable enough to keep test specs; pick `k-light-ui-testing` when the UI is still changing and specs would be discarded. If you cannot tell, ask the engineer. If `reply_channel_available` is `false` (an unattended run with no reply channel), default to `k-e2e-test-generation`. Its specs persist for re-use, where `k-light-ui-testing`'s prompts are meant to be discarded once a page stabilizes; a wrong default here costs a follow-up run, not test loss. Without `deployed_url` you MUST skip both and record in Step 11 that functional coverage was not exercised.
  - `k-e2e-test-generation`: set `url` to `deployed_url` and `output_mode` to `cypress` (or `playwright` if the project already uses Playwright). Step 7 only guarantees a unit/integration test framework (Jest, pytest, JUnit), never a Cypress or Playwright E2E project, so `project_mode` MUST NOT be assumed, and it MUST NOT be detected from the test directory recorded on the checklist in Step 7. That is the unit/integration test directory (`test/`, `__tests__/`), not where E2E config conventionally lives.
    - **Reuse before detecting:** check via the `sop-state-management` skill's Read/Write Opaque Data operation first for an `e2e_project_dir` already recorded for this run: `run.e2e_project_dir` under `branch` isolation, this feature's `features.<feature-slug>.e2e_project_dir` under `worktree` isolation. Under `feature_isolation=branch`, features share one working tree sequentially, so a value recorded by an earlier feature can apply here too. But only after verifying it: confirm the recorded path still exists and still contains a `cypress.config.*` or `playwright.config.*` file on the branch now checked out. `k-e2e-test-generation`'s own commit step commits the bootstrap to the feature branch that created it; if the current feature's branch descends from that branch, the config is present and the check passes. Set `project_mode` to `add-to-existing` and `project_dir` to the recorded value without re-scanning. If the recorded path is missing or the config file is absent, for example because this feature's branch was created from the project's base branch rather than from a prior feature's branch, the recorded value does not apply to this feature; fall through to **Detect otherwise** below instead of reusing a stale path. Under `feature_isolation=worktree`, each feature has its own separate working tree, so this reuse check is scoped to this feature's own prior Step 9 attempts only, never to another feature's worktree.
    - **Detect otherwise:** search these conventional E2E config locations for `cypress.config.*` or `playwright.config.*`, covering `.js`, `.ts`, `.mjs`, `.cjs`: the repository root, `./e2e/`, `./cypress/`, `./playwright/`, `./tests/e2e/`.
      - **Exactly one match:** set `project_mode` to `add-to-existing` and `project_dir` to that config file's directory.
      - **No match:** set `project_mode` to `bootstrap`; pass no `project_dir`.
      - **More than one match, or otherwise ambiguous:** ask the engineer which to use. This is the same pattern every other engineer-input point in this SOP follows. If `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark this step BLOCKED on the checklist with the candidate locations named and stop, the same handling as Step 0's `project_description` gate.
    - Whichever branch resolves `project_mode`, verify it against `k-e2e-test-generation`'s own parameter contract before invoking it: that SOP requires `project_dir` when `project_mode=add-to-existing`, and requires it absent when `project_mode=bootstrap`. Never pass `project_dir` in `bootstrap` mode.
    - However `project_mode`/`project_dir` was resolved, by reuse, by detection, or by a fresh bootstrap, record `project_dir` via the `sop-state-management` skill's Read/Write Opaque Data operation, as `run.e2e_project_dir` under `branch` isolation, or as this feature's `features.<feature-slug>.e2e_project_dir` under `worktree` isolation, before moving on, so a later feature's Step 9 pass reuses it per the rule above instead of re-detecting from scratch and bootstrapping a second project.
    - In both modes, set `credentials_file` to this run's `credentials_file` parameter if set.
    - Under `feature_isolation=worktree` on a greenfield project, two or more features can each independently hit **No match** and bootstrap their own Cypress or Playwright scaffold. Each feature's pull request then adds the same conventional config files (`cypress.config.ts`, `package.json`, etc.), which is a merge conflict for whoever reconciles them. This is foreseeable, and not something this SOP resolves; it already disclaims merge outcomes above.
  - `k-light-ui-testing`: verifies behavior live, no test framework required. Set `url` to `deployed_url`, `features` to the feature's flows drawn from `requirements.md`, `output_dir` to `output_dir/{feature-name}/testing/ui-prompts/`, and `credentials_file` to this run's `credentials_file` parameter if set. Its default `output_dir` is `tests/page/` relative to the working directory, which would land the prompt files outside this run's `output_dir`.
- Without `credentials_file`, a login-gated `deployed_url` is discovered as its login page and nothing behind it. Record functional coverage as not exercised beyond the login page rather than exercised, in either case.
- For a service, library, or data pipeline with no UI, the unit and integration tests from Step 7 plus this coverage analysis are the complete scope. You MUST NOT invent a UI test pass for a component that has no UI.
- If CRITICAL gaps are found, from the coverage analysis or from the security test plan's critical findings count, You MUST present them and MUST NOT proceed until the engineer chooses one of:
  - **(a) Already covered**: re-run this step with that context. If gaps clear, proceed. If not, ask again for (b) or (c).
  - **(b) Fix them**: spawn `k-developer` to write the missing tests, re-run the build, then re-run this step, reading and incrementing the feature's `fix_cycles_used` counter via the `sop-state-management` skill for the cycle. If Step 8 ran, update the affected pull request and re-run its fix cycle, up to `max_fix_cycles` from this run's shared per-feature budget, the same counter Steps 7 and 8 also read and increment, per the `max_fix_cycles` parameter definition above. If gaps remain after that, ask again for (c).
  - **(c) Accept them**: proceed, and list the accepted gaps in Step 11.
- If `reply_channel_available` is `false` (an unattended run with no reply channel), this gate has no safe default to assume silently. Unlike the scope questions above, a CRITICAL gap is a content risk, not a preference, and guessing (a) or (c) on the engineer's behalf could ship untested code. You MUST mark this step BLOCKED on the checklist for the affected feature and stop, the same handling as Step 0's project_description gate.
- When features run in parallel, You MUST let every feature finish its analysis, then present all features' CRITICAL gaps together and take one choice per feature in a single exchange. Do not interrupt the engineer once per feature as each finishes. That is the same interruption the scope questions above were consolidated to avoid.
- Escalation only moves forward: (a) may escalate to (b) or (c); (b) may escalate to (c).
- If Step 8 ran and gaps affect a reviewed feature, You MUST set that feature's verdict to NEEDS_REVALIDATION in `output_dir/{feature-name}/code-review/status.md`. Step 11 reports it from there.
- The `k-e2e-test-generation` UI pass above is a second, independent path that can also invalidate a Step 8 approval, and the NEEDS_REVALIDATION rule covers it too: when `output_mode` is `cypress` or `playwright`, that SOP's own Step 6 ("Commit Generated Tests") commits and pushes the generated test files onto the current feature branch whenever it finds changes to commit. Per that SOP's own Step 6, when reached through this SOP's Step 9, the feature's pull request from Step 8 already exists, so that push lands directly on it. This happens whether or not any CRITICAL gap was found. A clean coverage pass can still bootstrap or extend a whole test project that `k-code-review-workflow` never saw. If Step 8 ran and `k-e2e-test-generation` actually committed (i.e. its own Step 6 did not skip the commit because `git status --porcelain` reported no changes) for a feature whose Step 8 verdict is APPROVED, You MUST set that feature's verdict to NEEDS_REVALIDATION in `output_dir/{feature-name}/code-review/status.md` the same way. New, unreviewed content landed on an already-approved pull request. This does not apply to `k-light-ui-testing` or to `k-e2e-test-generation` run with `output_mode=unit-prompts`: neither commits anything (per their own Quality Gates, prompt files are reference material for a human, not source code), so neither can invalidate a review this way.
- Record the `testing` phase's outcome via the `sop-state-management` skill only once every feature's coverage analysis (and UI pass, if run) is resolved, per Progress Tracking's per-feature completeness rule.

**Expected Output:** Coverage and test-strategy reports in `output_dir/{feature-name}/testing/`, plus generated functional tests when the UI pass ran.

### 10. Documentation

Spawn `k-developer` to write project documentation, then `k-architect` to validate it.

**Constraints:**

- You MUST skip this step if `documentation` is in `skip_phases`.
- If the source directory is not recorded on the checklist on a greenfield project, for example because `implementation` is in `skip_phases`, apply Step 8's guard above: stop and ask the engineer for the source directory, or, if `reply_channel_available` is `false` (an unattended run with no reply channel), mark this step BLOCKED.
- You MUST write `README.md` to the project root, `codebase_path` on a brownfield project, or the source directory recorded on the checklist in Step 7 on a greenfield project, since on greenfield that directory, not `codebase_path` itself, is the project root Step 7 initialized, with these sections, in this order, omitting any that do not apply. The README ships with the project, so it belongs at that project root alongside the code, not under `output_dir`:
  1. Title and one-line description of what the project does
  2. Table of Contents
  3. Why this exists: the problem it solves, in two or three sentences
  4. Quick Start: prerequisites, install, and the first command that produces a visible result
  5. Usage Examples: two or three concrete invocations with their output
  6. Configuration: every environment variable and config file, each with its default
  7. Project Structure: an annotated tree of the directories that matter
  8. Optional Integrations: anything not required to run
  9. Contributing, Security, License
- You MUST write `developer-guide.md` to that same project root as the README above with these sections, in this order. Like the README, it is a project deliverable a contributor expects to find in the repo (the same category as a `CONTRIBUTING.md`), not an SDLC process record, so it goes to the project root rather than `output_dir`:
  1. Architecture: how the pieces fit, drawn from Step 3's design document
  2. Prerequisites: tools and versions needed to develop, not just to run
  3. Local Development Loop: build, run, test, and how to iterate quickly
  4. How to Add a Feature: the concrete path through the codebase, referencing Step 6's spec structure
  5. Test Strategy: what is unit-tested versus integration-tested, from Step 9
  6. Troubleshooting: the failures a new developer actually hits first
- If the repository root has no `AGENTS.md`, You MUST also produce one using `agents-md-authoring`. Its own procedure calls for showing proposed content and waiting for confirmation before writing. For this SOP's largely unattended run, treat this step's own completion gate (validated by `doc-accuracy-analyzer` and `humanize-writing`, both of which run next per this step) as that confirmation, and proceed without an additional pause.
- You MUST instruct `k-architect` to validate both with `doc-accuracy-analyzer` against the implementation, and to fix every INCORRECT finding before this step completes. On an INCORRECT finding, You MUST read this run's `run.doc_fix_cycles_used` counter via the `sop-state-management` skill, spawn `k-developer` to fix it and re-run `doc-accuracy-analyzer`, then increment and write back the counter through that skill, up to `max_fix_cycles` from this run's fix-cycle budget. This counter is its own field, separate from Steps 7, 8, and 9's shared per-feature `fix_cycles_used`: Step 10's documentation is project-wide, not per-feature, so there is no feature slug to attach it to. If the budget is exhausted and INCORRECT findings remain, You MUST mark this step BLOCKED on the checklist with the unresolved findings recorded, then stop and surface it to the engineer rather than proceeding with unvalidated documentation, the same handling as Step 0's `project_description` gate, and the same failure-mode this budget already prevents in Steps 7 through 9. This does not change `run.run_status` handling: Step 11 already leaves it `"in_progress"` whenever any phase is left BLOCKED, and an exhausted documentation fix-cycle budget is just one more way a phase can end up BLOCKED rather than COMPLETED.
- You MUST have `k-architect` apply `humanize-writing` to both artifacts as part of that same validation pass. The agent that authored the draft MUST NOT be the one that validates and humanizes it, preserving maker-checker separation. Both MUST meet this bar:
  - Short sentences. No filler, no marketing language, no "leverage" / "seamless" / "robust".
  - State what a thing does and why, not how it came to be. No changelog narration.
  - Every claim verifiable against the code. No hedging, no aspirational features.
  - Write for an engineer who joined today and needs to run the project this hour.
- You MUST NOT restate the design document in either artifact. Link to it.
- Every command you document MUST be one you actually ran in Step 7 or Step 9.
- Record the `documentation` phase's outcome via the `sop-state-management` skill at the same point you update the checklist: `COMPLETED` with a one-line summary once both artifacts are validated with 0 INCORRECT findings, `BLOCKED` otherwise.

**Expected Output:** `README.md` and `developer-guide.md` at the project root (`codebase_path` on a brownfield project, the Step 7 source directory on a greenfield project), both validated.

### 11. Final Summary

Spawn `k-developer` to consolidate every phase into one status report and write it.

**Constraints:**

- You MUST report each phase as COMPLETED, SKIPPED, or BLOCKED. This human-facing summary has only these three states, unlike the skill's own `phases.<phase-id>.status`, which also has `PENDING` and `IN_PROGRESS`. By the time this step runs the earlier phases have each reached a terminal state, except a per-feature phase (Steps 6 through 9) whose skill-tracked status is still `IN_PROGRESS` because at least one feature never cleared it, per Progress Tracking's per-feature completeness rule, that phase-level entry does not flip to `BLOCKED` on its own just because the run is ending. Report any such phase as BLOCKED in this summary regardless of what the skill's own entry still says, and name the specific feature(s) still open.
- You MUST list every generated artifact with its path, including the two `docs/design/` (or `validation_output_dir`, if the engineer overrode it) reports whose paths Step 3 recorded on the checklist, Step 10's `README.md` and `developer-guide.md` at the project root (`codebase_path` on a brownfield project, the Step 7 source directory on a greenfield project), the repository-root `AGENTS.md` from Step 10 if one was produced, and, for any feature where Step 9 ran `k-e2e-test-generation` in `add-to-existing` mode, the project test directory it wrote generated specs into. All of these are repo-root-relative escapes from `output_dir` and belong in this list for the same reason.
- You MUST surface all unresolved CRITICAL findings, every accepted test gap from Step 9, and any pull request left BLOCKED or NEEDS_REVALIDATION.
- **Evaluate every condition in the Quality Gate below against this run before writing anything.** This excludes `final-summary`'s own status, per that gate's own note, since this step is still in progress at the moment of evaluation. This decides the `satisfied` argument for Finalize Run below and the open items this summary reports; it is a pure read, with no write of its own. Once decided, write `sdlc-summary.md` first, delegating the write to the subagent (`k-developer`) and passing the exact summary content to write. You alone decide what the summary contains; the subagent performs the write to `output_dir/sdlc-summary.md`. `sdlc-summary.md` is written regardless of whether `satisfied` is true or false. It is this run's honest report of its own state at the time of writing either way. Only once that write is confirmed successful, You MUST record the `final-summary` phase via the skill's Read/Write a Phase Entry operation: `COMPLETED` with a one-line summary if `satisfied` is true, since the skill's own write contract requires a summary on every `COMPLETED` write and omitting one is a CRITICAL violation of that skill's Quality Gate, or `BLOCKED` if `satisfied` is false, the same state Progress Tracking's per-feature completeness rule already uses for a phase that has not actually cleared. Writing `BLOCKED` rather than `COMPLETED` when unsatisfied matters for resume: a later invocation's Resume Discovery adopts the resumed session's `phases` map as-is (per Progress Tracking's Phase list bullet), so a `final-summary` entry left `COMPLETED` here would read as already done even after whatever left the run unsatisfied is later fixed, and this step would never run again to regenerate `sdlc-summary.md` against the corrected state; `BLOCKED` leaves that signal in place. Then write `sdlc-checklist.md` to reflect it. The skill's fixed write order (state file first, then the paired human-readable file) applies here the same as everywhere else in this SOP. Writing the summary file before the phase entry that reports it as done matches every other phase in this SOP: each of Steps 0 through 10 already has its own artifact on disk before its phase-completion write happens, because that artifact is what the completion decision is validating; `final-summary` is not an exception to that pattern. A crash between marking the phase entry and writing `sdlc-summary.md` would otherwise leave a resumed session, which adopts its `phases` map as-is, believing the summary exists when it does not. **Only after `sdlc-summary.md`, the `final-summary` phase entry, and `sdlc-checklist.md` are all durably written** You MUST call the `sop-state-management` skill's Finalize Run operation, with `satisfied=true` if and only if every applicable condition held, `satisfied=false` otherwise. A run left BLOCKED on any phase or feature never becomes `completed`. Calling Finalize Run any earlier is unsafe, not merely out of order: it sets `run.run_status` to `completed`, and Resume Discovery's own match condition excludes any session whose `run_status` is already `completed`. So a crash between an early Finalize Run call and the writes above would permanently strand the session as unresumable while some or all of `sdlc-summary.md`, the phase entry, and `sdlc-checklist.md` still do not exist, with no way for a later invocation to ever pick it back up. Delegate every write in this bullet, Finalize Run included, to the subagent (`k-developer`), performing them as sequential dependent writes in the order given, never batched or run in parallel.
- **Completion criterion.** Step 11 is not complete until the Finalize Run call above has actually happened and returned success. Writing `sdlc-summary.md` and the `final-summary` phase entry stages that completion; it does not constitute it. If this step ends, by crash, by early exit, or by any other path, before Finalize Run has run, treat Step 11 as unfinished regardless of what is already on disk, and finish it by calling Finalize Run, not by re-writing files that already exist.

**Expected Output:** `output_dir/sdlc-summary.md` with phase statuses, artifacts, and open items, plus a confirmed Finalize Run call recording the run's outcome.

## Quality Gate

The pass is complete when all of the following hold. Each condition applies only if its phase ran. A phase in `skip_phases` cannot fail its own gate:

- Every phase in scope reports COMPLETED or SKIPPED, **excluding `final-summary` itself**. Step 11 evaluates this gate to decide the run's own outcome while it is still running, so `final-summary` cannot yet be COMPLETED at the moment of evaluation; it is marked COMPLETED immediately afterward, once Step 11 finishes writing the summary, per that step's own constraint. A phase may skip itself for its own reasons, Step 0 when the description is already clear, Step 1 when there is no existing code, and that is not a failure.
- If the design review ran: **0 unresolved CRITICAL findings**
- If implementation ran: every feature's build and tests pass
- If code review ran: every pull request is APPROVED, or its BLOCKED / NEEDS_REVALIDATION state is recorded in `sdlc-summary.md`
- If testing ran: **0 CRITICAL gaps**, or each remaining gap explicitly accepted by the engineer
- If documentation ran: `README.md` and `developer-guide.md` exist, carry no INCORRECT accuracy findings, and meet Step 10's writing bar as enforced by that step's `doc-accuracy-analyzer` and `humanize-writing` passes

If any condition fails, You MUST NOT report the pass as complete. Present the open items and ask the engineer how to proceed. If `reply_channel_available` is `false` (an unattended run with no reply channel), You MUST mark the run BLOCKED on the checklist with the open items recorded and stop rather than waiting indefinitely, the same handling as Step 0's `project_description` gate.
